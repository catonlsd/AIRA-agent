# Screenshots

Drop portfolio captures here and reference them from the root `README.md`. Each is
reachable in under a minute after **Seed demo data** in the operator console — see
[../DEMO_WALKTHROUGH.md](../DEMO_WALKTHROUGH.md).

| File | Capture | How to reach it |
|---|---|---|
| `operator-readiness.png` | Operator console → External sync panel | Incidents tab, after seeding |
| `observability.png` | Sync observability panel (SLOs, trends, alert) | top of Incidents tab |
| `drift-recovery.png` | A drifted incident's Refresh + Apply | expand the "ingest-worker" incident |
| `demo-tour.png` | The seeded guided tour | Demo data card, after seeding |
| `chat.png` | Chat product: streamed answer + artifact | the user product at `/` |

> These are placeholders; the `.png` files are intentionally not committed. Capture at a
> consistent width (≥ 1280px) on the demo data so the views match the documentation.
